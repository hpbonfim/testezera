name: Run Simple Preflight Build

on:
  pull_request:
    branches: ["staging"]

  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run simple preflight build on frontend
        run: docker build ./frontend --file Dockerfile.test
        with:
          context: .